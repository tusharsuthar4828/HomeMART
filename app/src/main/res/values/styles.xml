<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <!--selected things color-->
        <item name="colorPrimary">@color/green_800</item>
        <!--statusbar-->
        <item name="colorPrimaryDark">@color/white</item>
        <!---->
        <item name="colorAccent">@color/pink_500</item>

        <item name="colorControlActivated">@color/green_800</item>
        <item name="android:fontFamily">@font/roboto_light</item>
        <item name="android:windowActivityTransitions">true</item>


    </style>

    <style name="EditextTheme">
        <!-- Used for the bottom line when not selected / focused -->
        <item name="color">@color/green_800</item>
        <item name="android:background">@drawable/editextbox</item>
        <!-- colorControlActivated & colorControlHighlight use the colorAccent color by default -->
    </style>


    <style name="raisedbutton" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/green_800</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="raisedWhitebutton" parent="Widget.AppCompat.Button.Colored">
        <item name="backgroundTint">@color/white</item>
        <item name="android:background">@drawable/backgroundbutton</item>

    </style>

    <style name="flatbutton" parent="Widget.AppCompat.Button.Borderless" />


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="NavDrawerTextStyle" parent="Base.TextAppearance.AppCompat">
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">16sp</item>
    </style>


</resources>
